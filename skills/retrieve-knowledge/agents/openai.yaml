interface:
  display_name: "Retrieve Knowledge"
  short_description: "Packet-first typed wiki retrieval before implementation"
  default_prompt: "Use $retrieve-knowledge to fetch typed wiki context packets, rely on current packet guidance before recomputing architecture from code, and keep evidence checkpoints compact."
